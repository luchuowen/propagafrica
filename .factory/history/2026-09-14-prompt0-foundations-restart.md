# Session 2026-09-14 — Prompt 0: foundations restart

Direction C, "Living Systems", is locked. This supersedes the "Blueprint" build recorded
below in its entirety — new palette, new logo mark, new type system, new IA, new product
line (the client's actual flyer, not the earlier speculative "supplies" stages).

- **Palette** (`src/styles/tokens.css`): `--paper #fbf6ee`, `--ink #20241a`,
  `--soft #5c6350`, `--green #1f5c3f`, `--green-dark #163f2b`, `--terra #c1552b`,
  `--gold #d9a441`, `--mint #eaf3ec`, plus `--white` and `--mint-bright` for the mark's
  dark tone. The old Blueprint tokens (`--ink-soft`, `--signal`, `--signal-ink`, `--rule`,
  `--green-deep`, `--green-mid`, `--green-pale`, `--ink-overlay*`) are kept, unchanged,
  only because pre-restart pages and their components still read them.
- **Typography**: Inter Variable only (400–800), no serif anywhere; JetBrains Mono
  reserved for spec values, SKUs, dimensions and pack sizes. `--f-display` is now an
  alias for Inter, kept so components on the old scale don't need touching this session.
- **Logo**: `Mark.astro` redrawn — a centre node, one root, three shoots, three accent
  dots — `tone="light"|"dark"`. `Wordmark.astro` is now two-tone ("Propag" + "Africa")
  with a flex-letter-span "TECHNOLOGIES" strap matched to the wordmark's width.
- **Nav**: Home / Products / Tools / Field Notes / About / Contact, plus a separate
  persistent "Request a Quotation" header CTA. `Footer.astro` reuses `NAV_ITEMS` and
  carries the new locked baseline copy (copyright, NAVAC GLOBAL credit, strap line).
- `blueprint.md` fully replaced with the client's real product portfolio (seven
  families) and full page-by-page copy deck — the sole content source from here on.
- Pre-restart pages deleted: `how-grafting-works`, `ordering`, `specifications`,
  `supplies` and its five stage pages. `src/components/supplies/StageNav.astro` is now
  unreferenced (left in place, not deleted — not asked for).
- New `Carousel.astro` island (autoplay, crossfade+scale, swipe, keyboard, reduced-motion)
  for the three homepage carousels the next session builds.
- 64 placeholder images generated by `scripts/generate-placeholders.mjs` at every path
  in `docs/image-manifest.md`; real photography drops in at the same filenames.

**Known, deliberate breakage pending later sessions** — this was a foundations-only
session; `index.astro`, `about.astro`, `404.astro` and `field-notes/[slug].astro` were
left untouched per the brief and still hold pre-restart copy, the old CTA wording and
links into the deleted `/supplies` and `/ordering` routes (including via
`SupplyStages.astro`, still mounted on the homepage). The homepage/product/tools/about
sessions that follow rebuild these against `blueprint.md` and pick up the new nav links
as they go. (Superseded: Prompts 1-5 have since rebuilt all of these.)

**CLAUDE.md invariants now stale, not rewritten this session** (only a Build Workbook
note was added, per the brief): "Mono ... eyebrows" contradicts the new scale (eyebrows
are now Inter 700, not mono); "One colour only, never two" contradicts the new two-tone
wordmark; the `--signal` orange rule describes a token that is no longer the live brand
accent. Fix these invariants in the QA session once the new pages have replaced the old.
